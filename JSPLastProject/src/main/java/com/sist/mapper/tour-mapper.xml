<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.tour-mapper">
	<select id="travelListData" resultType="TourVO" parameterType="hashmap">
		SELECT contentid, contenttypeid, areacode, addr1, firstimage, likecount, replycount, title
		FROM tour
		WHERE contenttypeid=#{contenttypeid} AND areacode=#{areacode}
		OFFSET #{start} ROWS FETCH NEXT 12 ROWS ONLY
	</select>
	<select id="travelTotalPage" resultType="int" parameterType="hashmap">
		SELECT CEIL(COUNT(*)/12.0)
		FROM tour
		WHERE contenttypeid=#{contenttypeid} AND areacode=#{areacode}
	</select>
</mapper>